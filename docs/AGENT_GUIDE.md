# Agent 执行指南

1. 先读取当前任务卡、相关模块和已有测试。
2. 编辑前说明修改范围；保持模块解耦。
3. 每完成任务卡，运行定向测试和全量回归，更新文档并创建本地提交。
4. 不执行远程推送、删除、历史重写或覆盖不可恢复数据，除非用户单独确认。
5. 测试数据使用明确前缀并记录；不要记录 API Key、Cookie、完整聊天或其他秘密。
6. 仅在有明确授权时调用真实模型或写入 SillyTavern。
