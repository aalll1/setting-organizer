# Setting Organizer

SillyTavern 设定整理器扩展。

当前已完成到 `TC-04` JSON Schema 与解析校验。扩展提供基础面板、文本输入、整理目标选择、Token 预算模式、本地设置保存、分析状态、mock 角色草稿、mock 世界书草稿、基础编辑、JSON 解析、字段规范化和错误码。此阶段不调用模型、不创建角色、不写入世界书，也不会修改任何 SillyTavern 数据。

## 安装

将 `setting-organizer` 目录放入 SillyTavern 的第三方扩展目录中，然后在 SillyTavern 中启用扩展。

## 当前功能

- 加载扩展入口。
- 显示基础设置面板。
- 输入设定文本。
- 选择整理目标：角色卡、世界书。
- 选择 Token 预算模式：轻量、标准、长篇、自定义。
- 使用浏览器本地存储保存输入和设置。
- 点击“开始分析”后生成 mock 角色草稿和世界书草稿。
- 展示总览、角色、世界书、警告标签页。
- 编辑角色字段、世界书标题、关键词和正文。
- 删除角色草稿或世界书条目。
- 启用 / 禁用世界书条目。
- 解析 JSON 或 Markdown 代码块包裹的 JSON。
- 对缺失字段、字符串关键词、异常 confidence 做规范化。
- 对非 JSON、顶层非对象、空结果返回错误码。

## 已知限制

- 尚未接入 SillyTavern API 探针。
- 尚未接入 AI 模型调用。
- 当前 schema 文件已建立，但尚未引入完整 JSON Schema 引擎。
- 尚未实现导出。
- 尚未实现备份或导入。

## 安全策略

第一版默认不覆盖、不删除、不修改已有角色或世界书。AI 输出在后续阶段也只会先进入草稿和预览流程，不能直接写入 SillyTavern。
